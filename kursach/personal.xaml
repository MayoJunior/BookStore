<Window x:Class="kursach.personal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kursach"
        mc:Ignorable="d"
        Title="personal" Height="600" Width="800">
    <Grid Background="#a882f5">
        <Grid.RowDefinitions>
            <RowDefinition Height="115*"></RowDefinition>
            <RowDefinition Height="487*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock x:Name="UserInfo" Margin="10,4,683.6,4.8" Width="100" Height="100"/>
        <DataGrid x:Name="dataShop" Margin="0,0.2,-0.4,0.4" Grid.Row="2">
        <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование товара" Binding="{Binding Name}" Width="600"></DataGridTextColumn>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="200"></DataGridTextColumn>
            </DataGrid.Columns>
    </DataGrid>
    </Grid>
</Window>
